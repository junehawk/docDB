# ============================================
# docDB - Dependencies
# ============================================
# 설치: pip install -r requirements.txt

# --- 문서 텍스트 추출 ---
pdfplumber>=0.10.0
PyPDF2>=3.0.0
python-docx>=0.8.11
python-pptx>=0.6.21
openpyxl>=3.1.0
xlrd>=2.0.1
beautifulsoup4>=4.12.0
chardet>=5.2.0
lxml>=4.9.0

# --- 로컬 임베딩 ---
sentence-transformers>=2.7.0
torch>=2.0.0
transformers>=4.30.0
tokenizers>=0.14.0

# --- 벡터 DB ---
chromadb>=0.4.0

# --- MCP 서버 ---
mcp[cli]>=1.0.0

# --- 증분 인덱싱 ---
# SQLite는 Python 내장

# --- 유틸리티 ---
PyYAML>=6.0
pydantic>=2.0.0
# python-dotenv>=1.0.0  # 현재 미사용
loguru>=0.7.0
tqdm>=4.66.0
numpy>=1.24.0

# --- HWP 추출 (설치 실패 시 fallback 사용) ---
olefile>=0.47

# --- OCR (스캔 PDF 텍스트 추출, 선택적 설치) ---
# 스캔 PDF에서 텍스트를 추출하려면 별도 설치 필요:
#   pip install -e ".[ocr]"    (pyproject.toml 기준)
#   또는 직접 설치:
#     pip install pdf2image pytesseract
#     brew install poppler tesseract tesseract-lang   (macOS)
#     apt install poppler-utils tesseract-ocr          (Linux)
# macOS에서는 pyobjc Vision 프레임워크가 자동으로 사용됩니다.

# --- 보안 ---
defusedxml>=0.7.0

# --- 하이브리드 검색 ---
rank_bm25>=0.2.2

# --- 한국어 형태소 분석 (BM25 토크나이징 개선, 선택) ---
# kiwipiepy>=0.17.0  # 미설치 시 공백 기반 토크나이징 사용
